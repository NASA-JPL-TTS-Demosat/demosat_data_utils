# Ticket 20 Reasoning

## Summary
Created human-readable pre-merge smoke-test notebook `examples/demosat_data_utils_smoke_test.ipynb` that loads `DemosatChannelFrame` and `DemosatEvrFrame` from committed sample CSVs and exercises spec #11 EHA/EVR exit-condition gates.

## Decisions
- Used `csv_path` constructor with `coerce=True` as the public CSV construction path, matching existing tests and `TtsDataFrame` API.
- Paths are absolute (`/home/agent/workspace/examples/data/...`) to ensure nbconvert execution works regardless of kernel working directory.
- Notebook built with `nbformat` to guarantee valid nbformat 4 cells with required `outputs` field.
- Tests cover:
  * Public imports
  * Column retention and day-of-year timestamp parsing `%Y-%jT%H:%M:%S.%f`
  * Semantics: LABEL_COL/VALUE_COL/DEFAULT_TIME_LABEL
  * EHA alarm styling: dnAlarmState/euAlarmState, case-insensitive, red-over-yellow, neutral
  * EVR column retention including `metadata`, level styling for all 8 levels including `SIM_ERROR`, safe unknown level
  * `filter_level` type preservation and order
  * `lad()` latest-per-channel and type preservation
  * Type preservation via copy, sort, .loc, .iloc
  * Legacy `EvrItem`/`EvrContainer` unchanged

## Trade-offs
- Absolute paths make notebook less portable but guarantee execution in CI/nbconvert context where cwd may differ.
- Filter test for type preservation was added implicitly via copy/sort; boolean indexing also preserves type due to `_constructor` hook.
- No explicit test for `status` column never used; styling logic is verified via sample rows where `status` differs from alarm states.

## Verification
- Notebook executes successfully with `jupyter nbconvert --to notebook --execute`.
- All acceptance criteria satisfied.

## Review fixes
- Changed CSV paths from absolute `/home/agent/workspace/examples/data/...` to notebook-relative `data/sim_eha_sample.csv` and `data/sim_evr_sample.csv` for portability; nbconvert executes with cwd set to notebook directory (`examples/`).
- Removed unused `import os` from load cell.
- Added explicit boolean-index filter type-preservation check for EHA (`eha[eha['channelId']=='ADCS-0001']`) to satisfy "Type preservation demonstrated through copy, filter, sort, .loc, .iloc" criterion.
- Verified notebook still executes end-to-end after changes.

